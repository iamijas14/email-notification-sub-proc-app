<?xml version="1.0" encoding="utf-8"?>
<Configuration>

    <Appenders>

        <!-- Existing File Appender -->
        <RollingFile name="file"
            fileName="${sys:mule.home}${sys:file.separator}logs${sys:file.separator}abc-travels-email-notification-sub-proc.log"
            filePattern="${sys:mule.home}${sys:file.separator}logs${sys:file.separator}abc-travels-email-notification-sub-proc-%i.log">
            
            <PatternLayout pattern="%-5p %d [%t] [processor: %X{processorPath}; event: %X{correlationId}] %c: %m%n"/>
            <SizeBasedTriggeringPolicy size="10 MB"/>
            <DefaultRolloverStrategy max="10"/>
        </RollingFile>

        <Socket name="logstash"
                host="localhost"
                port="5000"
                protocol="TCP"
                immediateFail="false"
                reconnectDelayMillis="5000">

            <JsonLayout
                complete="false"
                compact="true"
                eventEol="true"
                properties="true"
                stacktraceAsString="true"/>
        </Socket>

    </Appenders>

    <Loggers>

        <!-- HTTP Logs -->
        <AsyncLogger name="org.mule.service.http" level="WARN"/>
        <AsyncLogger name="org.mule.extension.http" level="WARN"/>

        <!-- Mule Logger -->
        <AsyncLogger name="org.mule.runtime.core.internal.processor.LoggerMessageProcessor" level="INFO"/>

        <!-- Application Logs -->
        <AsyncRoot level="INFO">
            <AppenderRef ref="file"/>
            <AppenderRef ref="logstash"/>
			<AppenderRef ref="Console"/>
        </AsyncRoot>

    </Loggers>

</Configuration>
